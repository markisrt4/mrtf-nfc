<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- DND Mode -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- NFC -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="true" />

    <!-- BLUETOOTH -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <!-- Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:allowBackup="true"
        android:label="MRTF NFC"
        android:supportsRtl="true"
        android:theme="@style/Theme.MRTFApp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">

            <!-- Launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- MRTF v1: primary -->
            <intent-filter android:priority="0">
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/mrtf" />
            </intent-filter>

            <!-- Back-compat -->
            <intent-filter android:priority="0">
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/vnd.mrtechforge.mrtf" />
            </intent-filter>

            <!-- TECH route -->
            <intent-filter android:priority="0">
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc_tech_filter" />
        </activity>

        <!-- Action Activities (must be registered or routing can fail) -->
        <activity android:name=".actions.WifiActionActivity" android:exported="false" />
        <activity android:name=".actions.BluetoothActionActivity" android:exported="false" />
        <activity android:name=".actions.BedtimeActionActivity" android:exported="false" />
        <activity android:name=".actions.SosActivity" android:exported="false" />

        <!-- Debug -->
        <activity android:name=".debug.DebugPayloadActivity" android:exported="false" />

    </application>
</manifest>
